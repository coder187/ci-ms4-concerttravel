todo:
fix noimage AND IMAGE SETTINGS [done]
add br to index 
add ticket discount
toasts
size images
checkout.models.Order.update_total [done]
fix css on checkout css
fix country code on payment form
fix delivert details
remove print and console debug lines.
ensure all images are workung as expected
general css and site tidu yp
automaated testing.
fix images not showing on bag htnml [fixed]
resize images
add qty to message toats.
email text and possiblt attach ticket.
discount when ticket purch >10
date format on date picker
add name to profile page.
add if cond to events to show events where publish = false for superuser.
SWITCH DEBUG TO FALSE



force rebuild 4
qjuestuin:
siganl to update lineitem total on order header. Why not recalc as needed?
why requirements file so large?


useful commands ##################
pip3 freeze > unins.txt && pip3 uninstall -y -r unins.txt && rm unins.txt
pip3 install -r requirements.txt

python3 manage.py makemikgrations --dry-run 
python3 manage.py migrate --plan 

pip3 install psycopg2-binary


python3 manage.py dumpdata --exclude auth.permission --exclude contenttypes > db.json
python3 manage.py loaddata db.json
heroku run python3 manage.py migrate

user accs
testcheckout@gmail.com
testuser
$Qwerty1234



@register.filter(name='pad_ticket_number')
def pad_ticket_number(ticketNo, pad):
    """add leading zeros to ticket no"""
    return str(arg1) + ':' + str(arg2)


    4000 0027 6000 3184
    
    qww

